@model IEnumerable<MvcExamenCore2.Models.Libro>

@{
    ViewData["Title"] = "Libros";
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdLibro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Titulo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Autor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Editorial)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Portada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdGenero)
            </th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdLibro)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Titulo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Autor)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Editorial)
                </td>
                <td>
                    <img src="~/Assets/Imagenes/@item.Portada" style="width:200px; height:250px" />
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Precio)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdGenero)
                </td>
                <td>
                    <a asp-controller="Home" asp-action="DetallesLibro" asp-route-idLibro="@item.IdLibro" class="btn btn-info">Detalles</a>
                </td>
                <td>
                    @if (Context.User.Identity.IsAuthenticated)
                    {
                        <a asp-controller="Home" asp-action="InsertarCarrito" asp-route-idLibro="@item.IdLibro" class="btn btn-success">Carrito</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>

